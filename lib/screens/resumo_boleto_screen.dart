import 'package:flutter/material.dart';

class ResumoBoletoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
          title: Text('Boleto'),
          centerTitle: true,
       ),
       body: Container(
          color: Colors.black12,
       ),
    );
  }
}
