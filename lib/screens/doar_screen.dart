import 'package:flutter/material.dart';

class DoarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
     return Scaffold(
        appBar: AppBar(
           title: Text('Doar'),
           centerTitle: true,
        ),
        body: Container(color: Colors.blue),
     );
  }
}
